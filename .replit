run = "bash start.sh"
entrypoint = "start.sh"

[env]
PORT = "8080"

[deployment]
run = ["sh", "-c", "cd backend && uvicorn main:app --host 0.0.0.0 --port 8080"]
build = ["sh", "-c", "cd frontend && npm install && npm run build && mkdir -p ../backend/frontend && cp -r dist ../backend/frontend/"]

[[ports]]
localPort = 8080
externalPort = 80
